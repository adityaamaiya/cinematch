# deps
node_modules/

# build output
dist/
build/

# env — real secrets, never commit
.env
*.env
!.env.example

# scraper persistent browser session (contains logged-in cookies)
scraper/.moctale-session/

# scraper HTML dumps (selector tuning only)
*.dump.html

# logs / misc
*.log
.DS_Store
