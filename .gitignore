# deps
node_modules/

# build output
dist/
build/

# env — real secrets, never commit
.env
*.env
!.env.example

# logs / misc
*.log
.DS_Store

# personal Moctale data dump (not for the public repo)
moctale.response.json
backend/profile.local.json

# personal taste profile for the LLM mode — per-deployment, generate your own from your ratings
# (see taste-profile.example.md). Not committed.
backend/taste-profile.md
# preserved Opus baseline kept for the gen-taste diff — also personal, never committed.
backend/taste-profile.opus.md
